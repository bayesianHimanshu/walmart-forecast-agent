/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',        // produces a self-contained server for the container
  reactStrictMode: true,
};
module.exports = nextConfig;
